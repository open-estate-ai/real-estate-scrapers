# Example environment variables for local testing
# Do not commit real secrets. Keep this file out of source control.

TARGET_URL=https://example.com
BUCKETS=["LOCAL"]
# Custom variables for this scraper:
#API_KEY=your_api_key_here
#MAX_PAGES=10
